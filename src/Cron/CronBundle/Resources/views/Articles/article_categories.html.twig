{% extends '::base.html.twig' %}
{% block content %}
    <h2 class="categoryH2">{{ title }}</h2>
    <ul class="article-categories">
        {% for cat in categories %}
            <li><a href="/articles/{{ cat.getId() }}">{{ cat.getName() }}</a></li>
        {% endfor %}
    </ul>
{% endblock %}